//!SeaORM-based storage implementation for UserService
//!
//! Generated by protoc-gen-synapse from protobuf service definition.
//! @generated
#![allow(missing_docs)]
#![allow(unused_imports)]
use super::prelude::*;
use super::user_service_storage::{UserServiceStorage, StorageError};
use super::conversions::ApplyUpdate;
use sea_orm::{
    ActiveModelTrait, ColumnTrait, DatabaseConnection, EntityTrait, QueryFilter,
    QueryOrder, Set,
};
///SeaORM-based implementation of UserServiceStorage using DatabaseConnection
#[derive(Clone)]
pub struct SeaOrmUserServiceStorage {
    db: DatabaseConnection,
}
impl SeaOrmUserServiceStorage {
    /// Create a new storage implementation with the given database connection
    pub fn new(db: DatabaseConnection) -> Self {
        Self { db }
    }
    /// Get a reference to the database connection
    pub fn db(&self) -> &DatabaseConnection {
        &self.db
    }
}
#[async_trait::async_trait]
impl UserServiceStorage for SeaOrmUserServiceStorage {
    async fn get_user(
        &self,
        request: GetUserRequest,
    ) -> Result<GetUserResponse, StorageError> {
        let model = user::Entity::find_by_id(request.id)
            .one(&self.db)
            .await
            .map_err(StorageError::Database)?
            .ok_or_else(|| StorageError::NotFound(
                format!("{} with id {} not found", stringify!(User), request.id),
            ))?;
        Ok(GetUserResponse {
            user: Some(model.into()),
        })
    }
    async fn list_users(
        &self,
        request: ListUsersRequest,
    ) -> Result<UserConnection, StorageError> {
        use sea_orm::QuerySelect;
        let limit = request.first.or(request.last).unwrap_or(20) as u64;
        let mut query = user::Entity::find().order_by_asc(user::Column::Id);
        if let Some(ref after) = request.after {
            if let Ok(cursor_id) = after.parse::<i64>() {
                query = query.filter(user::Column::Id.gt(cursor_id));
            }
        }
        if let Some(ref before) = request.before {
            if let Ok(cursor_id) = before.parse::<i64>() {
                query = query.filter(user::Column::Id.lt(cursor_id));
            }
        }
        let models = query
            .limit(limit + 1)
            .all(&self.db)
            .await
            .map_err(StorageError::Database)?;
        let has_next_page = models.len() > limit as usize;
        let models: Vec<_> = models.into_iter().take(limit as usize).collect();
        let edges: Vec<UserEdge> = models
            .into_iter()
            .map(|m| {
                let cursor = m.id.to_string();
                UserEdge {
                    cursor,
                    node: Some(m.into()),
                }
            })
            .collect();
        let start_cursor = edges.first().map(|e| e.cursor.clone());
        let end_cursor = edges.last().map(|e| e.cursor.clone());
        Ok(UserConnection {
            edges,
            page_info: Some(PageInfo {
                has_next_page,
                has_previous_page: request.after.is_some(),
                start_cursor,
                end_cursor,
            }),
        })
    }
    async fn create_user(
        &self,
        request: CreateUserRequest,
    ) -> Result<CreateUserResponse, StorageError> {
        let input = request
            .input
            .ok_or_else(|| {
                StorageError::InvalidArgument("input is required".to_string())
            })?;
        let active_model: user::ActiveModel = input.into();
        let model = active_model.insert(&self.db).await.map_err(StorageError::Database)?;
        Ok(CreateUserResponse {
            user: Some(model.into()),
        })
    }
    async fn update_user(
        &self,
        request: UpdateUserRequest,
    ) -> Result<UpdateUserResponse, StorageError> {
        use sea_orm::IntoActiveModel;
        let input = request
            .input
            .ok_or_else(|| {
                StorageError::InvalidArgument("input is required".to_string())
            })?;
        let model = user::Entity::find_by_id(request.id)
            .one(&self.db)
            .await
            .map_err(StorageError::Database)?
            .ok_or_else(|| StorageError::NotFound(
                format!("{} with id {} not found", stringify!(User), request.id),
            ))?;
        let mut active_model = model.into_active_model();
        active_model.apply_update(input);
        let model = active_model.update(&self.db).await.map_err(StorageError::Database)?;
        Ok(UpdateUserResponse {
            user: Some(model.into()),
        })
    }
    async fn delete_user(
        &self,
        request: DeleteUserRequest,
    ) -> Result<DeleteUserResponse, StorageError> {
        let result = user::Entity::delete_by_id(request.id)
            .exec(&self.db)
            .await
            .map_err(StorageError::Database)?;
        Ok(DeleteUserResponse {
            success: result.rows_affected > 0,
        })
    }
}
